# Language setting
language: node_js

# Version number
node_js:
  - 0.12
  
env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:

  ci:
    - ps -eax
    - ls -al

integrations:
  hub:
    - integrationName: ship-docker
      type: docker 
    - integrationName: irc
      type: irc
      recipients: 
        - "chat.freenode.net#test-cock"
      on_start:
      on_success: always
      on_failure: always 
      branches:  
        only
          - irc1
