# Language setting
language: node_js

# Version number
node_js:
  - 0.12
  
env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:

  ci:
    - ps -eax
    - ls -al
   # - if [ "$SHIPPABLE_NODE_VERSION" == "0.12" ]; then foobar; fi

integrations:
    - integrationName: ship-slack
      type: slack
      recipients: 
        - "#hitchcock1-public"
        - pvt-channel
      on_start: always
      on_success: always
      on_failure: always         
    